# Changelog

{{version-tag-date}}

## Changes

- The home page now shows when DNS is not working, and on macOS can switch back to automatic DNS in one click.
- Fixed manually configured IPv6 DNS servers being reset after stopping TUN mode on macOS.

## 变更

- 主页网络状态会提示 DNS 异常，macOS 上可一键改回自动获取 DNS。
- 修复 macOS 停止 TUN 后手动设置的 IPv6 DNS 被重置的问题。
